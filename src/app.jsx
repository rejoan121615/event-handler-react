import React from "react";
import Home from "./page/Home";

const App = (props) => {
    return (
        <React.Fragment>
            <Home />
        </React.Fragment>
    );
};

export default App;
